#include <stdio.h>

// Function declarations (prototypes)
void biggest3();
void factorial();

int main() {
    biggest3();   // Call to biggest3 function
    factorial();  // Call to factorial function
    return 0;     // Return 0 to indicate successful execution
}
